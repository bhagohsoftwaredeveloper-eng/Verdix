-- Create x_readings table for storing X-reading reports (cashier shift reports)
CREATE TABLE IF NOT EXISTS x_readings (
  id INT AUTO_INCREMENT PRIMARY KEY,
  reading_number VARCHAR(50) NOT NULL UNIQUE,
  report_date DATETIME NOT NULL,
  shift_start DATETIME,
  shift_end DATETIME,
  terminal_id VARCHAR(50),
  cashier_name VARCHAR(100),
  cashier_id VARCHAR(50),
  gross_sales DECIMAL(15, 2) NOT NULL DEFAULT 0.00,
  returns DECIMAL(15, 2) NOT NULL DEFAULT 0.00,
  discounts DECIMAL(15, 2) NOT NULL DEFAULT 0.00,
  net_sales DECIMAL(15, 2) NOT NULL DEFAULT 0.00,
  vat_amount DECIMAL(15, 2) NOT NULL DEFAULT 0.00,
  payment_methods JSON,
  transaction_count INT NOT NULL DEFAULT 0,
  starting_cash DECIMAL(15, 2) NOT NULL DEFAULT 0.00,
  cash_sales DECIMAL(15, 2) NOT NULL DEFAULT 0.00,
  cash_in_drawer DECIMAL(15, 2) NOT NULL DEFAULT 0.00,
  shift_status ENUM('active', 'completed') DEFAULT 'active',
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  INDEX idx_report_date (report_date),
  INDEX idx_shift_start (shift_start),
  INDEX idx_shift_end (shift_end),
  INDEX idx_terminal_id (terminal_id),
  INDEX idx_cashier_id (cashier_id),
  INDEX idx_shift_status (shift_status),
  INDEX idx_created_at (created_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- Insert sample data for testing
INSERT INTO x_readings (
  reading_number,
  report_date,
  shift_start,
  shift_end,
  terminal_id,
  cashier_name,
  cashier_id,
  gross_sales,
  returns,
  discounts,
  net_sales,
  vat_amount,
  payment_methods,
  transaction_count,
  starting_cash,
  cash_sales,
  cash_in_drawer,
  shift_status
) VALUES
(
  'X-2026-01-13-001',
  '2026-01-13 14:30:00',
  '2026-01-13 09:00:00',
  '2026-01-13 17:00:00',
  'POS-001',
  'John Doe',
  'cashier_001',
  7200.75,
  -45.00,
  -30.00,
  7125.75,
  762.38,
  '[{"name":"Cash","amount":3800.00},{"name":"Credit Card","amount":2100.25},{"name":"GCash","amount":1225.50}]',
  28,
  5000.00,
  3800.00,
  8800.00,
  'completed'
),
(
  'X-2026-01-13-002',
  '2026-01-13 10:15:00',
  '2026-01-13 06:00:00',
  NULL,
  'POS-001',
  'Jane Smith',
  'cashier_002',
  1250.50,
  0.00,
  -15.00,
  1235.50,
  132.22,
  '[{"name":"Cash","amount":800.00},{"name":"Credit Card","amount":435.50}]',
  8,
  2000.00,
  800.00,
  2800.00,
  'active'
);